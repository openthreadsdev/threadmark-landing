---
import BaseLayout from '../layouts/BaseLayout.astro';
import { siteConfig } from '../config';
---

<BaseLayout
	title="Threadmark for EU Shopify Merchants"
	description="Stop tracking product compliance in spreadsheets. Threadmark gives Shopify merchants a single place to manage, update, and export compliance data for EU regulations."
>
	<!-- Hero -->
	<section class="hero-wrapper dot-grid-bg">
		<div class="container">
			<div class="hero">
				<p class="hero-audience">For Shopify merchants selling physical products into the EU</p>
				<h1>Product compliance, organised and audit-ready</h1>
				<p class="hero-sub">
					One place to manage compliance data, track changes, and export reports — synced with your Shopify catalogue.
				</p>
				<div class="hero-cta">
					<a href="#waitlist" class="btn btn-primary btn-lg">Join the waitlist</a>
					<a href={siteConfig.calendarUrl} target="_blank" rel="noopener noreferrer" class="btn btn-secondary">Book a call</a>
				</div>
			</div>
		</div>
	</section>

	<!-- Why it matters -->
	<section class="benefits-section">
		<div class="container">
			<h2>Why it matters</h2>
			<ul class="benefits-list">
				<li>
					<strong>Spend less time on compliance admin</strong>
					<span>Stop chasing documents across spreadsheets, emails, and shared drives.</span>
				</li>
				<li>
					<strong>Be ready when auditors come knocking</strong>
					<span>Export a complete compliance report for any product in seconds.</span>
				</li>
				<li>
					<strong>Reduce the risk of non-compliance</strong>
					<span>Structured records and change tracking mean nothing falls through the cracks.</span>
				</li>
			</ul>
		</div>
	</section>

	<!-- How it works -->
	<section class="how-section">
		<div class="container">
			<h2>How it works</h2>
			<ol class="steps">
				<li>
					<span class="step-num">1</span>
					<div>
						<strong>Connect your store</strong>
						<p>Link your Shopify catalogue and your products appear automatically.</p>
					</div>
				</li>
				<li>
					<span class="step-num">2</span>
					<div>
						<strong>Organise your compliance data</strong>
						<p>Add materials, certifications, and documentation for each product.</p>
					</div>
				</li>
				<li>
					<span class="step-num">3</span>
					<div>
						<strong>Export when you need it</strong>
						<p>Generate audit-ready reports and share them with anyone who needs them.</p>
					</div>
				</li>
			</ol>
		</div>
	</section>

	<!-- Features -->
	<section class="features-section">
		<div class="container">
			<h2>What's included</h2>
			<ul class="features-grid">
				<li>Automatic product sync from Shopify</li>
				<li>Compliance data editor with validation</li>
				<li>Multi-material composition tracking</li>
				<li>Certification and document attachments</li>
				<li>Full audit trail on every change</li>
				<li>JSON and PDF export with versioning</li>
				<li>QR codes for public compliance pages</li>
				<li>Compliance status dashboard</li>
			</ul>
		</div>
	</section>

	<!-- FAQ -->
	<section class="faq-section">
		<div class="container">
			<h2>Frequently asked questions</h2>
			<dl class="faq-list">
				<div class="faq-item">
					<dt>Which EU regulations does this cover?</dt>
					<dd>
						Threadmark helps you organise and export the compliance data you already need
						to maintain — materials, certifications, and product documentation. It does not
						provide legal advice or automated regulatory checks.
					</dd>
				</div>
				<div class="faq-item">
					<dt>How many products can I manage?</dt>
					<dd>
						Threadmark is designed for merchants with 5–200 SKUs. If you have more, get in
						touch and we'll work out the right fit.
					</dd>
				</div>
				<div class="faq-item">
					<dt>Does it work with my existing Shopify setup?</dt>
					<dd>
						Yes. Threadmark installs as a Shopify embedded app and syncs with your product
						catalogue. No theme changes required.
					</dd>
				</div>
				<div class="faq-item">
					<dt>What does it cost?</dt>
					<dd>
						Pricing will be announced closer to launch. Join the waitlist to be the first
						to know.
					</dd>
				</div>
				<div class="faq-item">
					<dt>Is my data safe?</dt>
					<dd>
						All data is isolated per store, with role-based access control and a full audit
						trail. We never share your data with third parties.
					</dd>
				</div>
			</dl>
		</div>
	</section>

	<!-- Trust signal -->
	<section class="trust-section">
		<div class="container">
			<p>
				Built by a team with direct experience in EU product compliance.
				Threadmark is designed to solve one problem well — giving merchants
				a reliable way to manage and export the compliance data they already need.
			</p>
		</div>
	</section>

	<!-- Waitlist form -->
	<section class="waitlist-section" id="waitlist">
		<div class="container">
			<h2>Ready to get organised?</h2>
			<p>Join the waitlist for early access when Threadmark launches.</p>
			<form
				name="waitlist-eu-merchant"
				method="POST"
				data-netlify="true"
				action="/thanks"
			>
				<input type="hidden" name="form-name" value="waitlist-eu-merchant" />
				<div class="form-row">
					<label for="email-eu" class="sr-only">Email address</label>
					<input
						type="email"
						id="email-eu"
						name="email"
						required
						placeholder="you@example.com"
						autocomplete="email"
					/>
					<button type="submit" class="btn btn-primary btn-lg">Join waitlist</button>
				</div>
				<p class="form-reassurance">No spam — just a heads up when we're ready.</p>
				<p class="form-notice"><a href="/privacy">Privacy Policy</a></p>
			</form>
		</div>
	</section>
</BaseLayout>

<style>
	/* Hero */
	.hero-wrapper {
		padding-top: 4rem;
	}

	.hero {
		padding: 0 0 3rem;
		max-width: 40rem;
	}

	.hero-audience {
		font-size: 0.8125rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		color: var(--color-text-muted);
		margin-bottom: 0.75rem;
	}

	.hero h1 {
		margin-bottom: 1rem;
	}

	.hero-sub {
		font-size: 1.0625rem;
		color: var(--color-text-secondary);
		margin-bottom: 2rem;
	}

	.hero-cta {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		align-items: center;
	}

	.btn {
		display: inline-block;
		padding: 0.75rem 1.5rem;
		min-height: 2.75rem;
		border-radius: 0.375rem;
		font-weight: 600;
		font-size: 0.9375rem;
		text-decoration: none;
		text-align: center;
		transition: background-color 0.15s ease;
	}

	.btn-lg {
		padding: 0.875rem 2rem;
		font-size: 1rem;
	}

	.btn-primary {
		background: var(--color-accent);
		color: #fff;
	}

	.btn-primary:hover {
		background: var(--color-accent-hover);
	}

	.btn-secondary {
		background: var(--color-surface);
		color: var(--color-text);
		border: 1px solid var(--color-input-border);
	}

	.btn-secondary:hover {
		border-color: var(--color-text);
	}

	/* Benefits */
	.benefits-section {
		padding: 4rem 0;
		border-top: 1px solid var(--color-border);
	}

	.benefits-section h2 {
		margin-bottom: 1.5rem;
	}

	.benefits-list {
		list-style: none;
		display: grid;
		gap: 1.5rem;
	}

	.benefits-list li {
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
	}

	.benefits-list strong {
		font-size: 1.0625rem;
	}

	.benefits-list span {
		color: var(--color-text-muted);
		font-size: 0.9375rem;
	}

	/* How it works */
	.how-section {
		padding: 4rem 0;
		border-top: 1px solid var(--color-border);
		max-width: 40rem;
		margin: 0 auto;
	}

	.how-section h2 {
		margin-bottom: 2rem;
	}

	.steps {
		list-style: none;
		display: grid;
		gap: 1.5rem;
		max-width: 40rem;
	}

	.steps li {
		display: flex;
		gap: 1rem;
		align-items: flex-start;
	}

	.step-num {
		display: flex;
		align-items: center;
		justify-content: center;
		width: 2rem;
		height: 2rem;
		border-radius: 50%;
		background: var(--color-step-bg);
		font-weight: 700;
		font-size: 0.875rem;
		flex-shrink: 0;
	}

	.steps strong {
		display: block;
		margin-bottom: 0.125rem;
	}

	.steps p {
		color: var(--color-text-muted);
		font-size: 0.9375rem;
	}

	/* Features */
	.features-section {
		padding: 4rem 0;
		border-top: 1px solid var(--color-border);
	}

	.features-section h2 {
		margin-bottom: 1.5rem;
	}

	.features-grid {
		list-style: none;
		display: grid;
		gap: 0.75rem;
	}

	.features-grid li {
		padding: 0.75rem 1rem;
		background: var(--color-surface-alt);
		border-radius: 0.375rem;
		font-size: 0.9375rem;
	}

	/* FAQ */
	.faq-section {
		padding: 4rem 0;
		border-top: 1px solid var(--color-border);
		max-width: 40rem;
		margin: 0 auto;
	}

	.faq-section h2 {
		margin-bottom: 1.5rem;
	}

	.faq-list {
		display: grid;
		gap: 1.5rem;
	}

	.faq-item dt {
		font-weight: 600;
		margin-bottom: 0.25rem;
	}

	.faq-item dd {
		color: var(--color-text-secondary);
		font-size: 0.9375rem;
	}

	/* Trust signal */
	.trust-section {
		padding: 4rem 0;
		border-top: 1px solid var(--color-border);
	}

	.trust-section p {
		color: var(--color-text-muted);
		font-size: 0.9375rem;
		font-style: italic;
		max-width: 40rem;
	}

	/* Waitlist */
	.waitlist-section {
		padding: 4rem 0;
		border-top: 1px solid var(--color-border);
		max-width: 40rem;
		margin: 0 auto;
	}

	.waitlist-section h2 {
		margin-bottom: 0.5rem;
	}

	.waitlist-section p {
		color: var(--color-text-muted);
		margin-bottom: 1.5rem;
	}

	.form-row {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
		max-width: 28rem;
	}

	.form-row input[type='email'] {
		width: 100%;
		min-height: 2.75rem;
		padding: 0.75rem 1rem;
		border: 1px solid var(--color-input-border);
		border-radius: 0.375rem;
		font-size: 1rem;
		font-family: inherit;
		background-color: var(--color-surface);
		color: var(--color-text);
	}

	.form-row input[type='email']:focus {
		outline: 2px solid var(--color-text);
		outline-offset: -1px;
		border-color: transparent;
	}

	.form-reassurance {
		margin-top: 1rem;
		font-size: 0.875rem;
		color: var(--color-text-secondary);
	}

	.form-notice {
		margin-top: 0.25rem;
		font-size: 0.8125rem;
		color: var(--color-text-muted);
	}

	.form-notice a {
		color: var(--color-text-muted);
		text-decoration: underline;
	}

	.form-notice a:hover {
		color: var(--color-text);
	}

	.sr-only {
		position: absolute;
		width: 1px;
		height: 1px;
		padding: 0;
		margin: -1px;
		overflow: hidden;
		clip: rect(0, 0, 0, 0);
		white-space: nowrap;
		border-width: 0;
	}

	@media (min-width: 640px) {
		.benefits-list {
			grid-template-columns: repeat(3, 1fr);
		}

		.features-grid {
			grid-template-columns: 1fr 1fr;
		}

		.form-row {
			flex-direction: row;
		}

		.form-row input[type='email'] {
			width: auto;
			flex: 1;
		}
	}
</style>
